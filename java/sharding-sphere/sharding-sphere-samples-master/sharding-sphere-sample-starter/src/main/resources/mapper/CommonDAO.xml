<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.setamv.shardingsphere.starter.dao.CommonDAO">

	<select id="executeDqlSql" parameterType="string" resultType="map">
		${sqlText}
	</select>

	<select id="executeDmlSql" parameterType="string" resultType="int">
		${sqlText}
	</select>

	<select id="executeDdlSql" parameterType="string">
		${sqlText}
	</select>
</mapper>