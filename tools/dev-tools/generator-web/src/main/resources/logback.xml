<!-- Logback configuration. See http://logback.qos.ch/manual/index.html -->
<!-- 
	scan：配置文件发生更改时，进行重载，默认值为true 
	scanPeriod：监测配置文件是否有修改的时间间隔，默认值为6000，默认单位为毫秒
	debug：打印logback内部日志信息，实时查看logback运行状态，默认值为false
-->
<configuration scan="true" scanPeriod="2 seconds">
	<!-- 配置教程：https://www.w3cschool.cn/article/2632622.html -->
	<!--定义日志文件的存储地址 -->
	<property name="LOG_PATH" value="D:/Temp/logs" />
	<!-- 控制台输出：appender是负责写日志的组件，通过自定义日志组件控制日志输出的情况，包含以下两个属性-->
	<!-- name：组件名称，class：组件class指定的类名 不同的class，对应不同的功能：-->
	<!-- ch.qos.logback.core.ConsoleAppender会把日志输出到控制台 -->
	<!-- ch.qos.logback.core.rolling.RollingFileAppender把日志内容输出到指定文件 -->
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<!--格式化输出：%d表示日期，%-5level：级别从左显示5个字符宽度，%t表示线程名，%msg：日志消息，%n是换行符 -->
			<pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}][%-5level][${PID:-}][%t] %logger{50} => %msg%n</pattern>
		</encoder>
	</appender>

	<!-- info级别日志文件输出 -->
	<appender name="INFO_FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<!-- 日志文件输出的文件名 -->
		<File>${LOG_PATH}/info.log</File>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<!-- 每日生成日志文件或日志文件大小超出限制后输出的文件名模板 -->
			<fileNamePattern>${LOG_PATH}/info-%d{yyyy-MM-dd}.%i.log
			</fileNamePattern>
			<!-- 日志文件保留天数 -->
			<maxHistory>30</maxHistory>
			<!-- 日志文件最大大小：100MB -->
			<maxFileSize>100MB</maxFileSize>
		</rollingPolicy>
		<encoder
			class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level ${PID:-} --- [%t] %logger{50} - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- error级别日志文件输出 -->
	<appender name="ERROR_FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<!-- 日志输出级别，优先级 > '<root level>' -->
		<!-- filter节点：过滤器，用来指定日志组件的日志输出级别，优先级高于root节点的level。 -->
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>ERROR</level>
		</filter>
		<File>${LOG_PATH}/error.log</File>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<fileNamePattern>${LOG_PATH}/error-%d{yyyy-MM-dd}.%i.log
			</fileNamePattern>
			<maxHistory>30</maxHistory>
			<maxFileSize>100MB</maxFileSize>
		</rollingPolicy>
		<encoder
			class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level ${PID:-} --- [%t]
				%logger{50} - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- 默认日志输出级别 -->
	<root level="INFO">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="INFO_FILE" />
		<appender-ref ref="ERROR_FILE" />
	</root>
</configuration>